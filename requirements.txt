# This project uses `uv` for dependency management.
# Dependencies are defined in pyproject.toml as the single source of truth.
#
# To install:
#   uv sync
#
# For environments without uv, use:
#   pip install -e .
#
# Alternatively, the pinned versions below can be used with pip:
pandas==2.3.3
python-dotenv==1.2.1
httpx==0.28.1
pydantic==2.12.5
tenacity==9.1.2
openai==2.14.0
google-generativeai==0.8.6
ollama==0.6.1